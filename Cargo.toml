[workspace]
members = [
    "vector_db_engine",
    "telegram_bot"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
split-debuginfo = "off"
strip = true
